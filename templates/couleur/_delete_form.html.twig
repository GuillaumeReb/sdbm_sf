<form method="post" action="{{ path('app_couleur_delete', {'id': couleur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ couleur.id) }}">
    <button class="btn">Delete</button>
</form>
