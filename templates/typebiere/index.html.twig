{% extends 'base.html.twig' %}

{% block title %}Typebiere index{% endblock %}

{% block body %}
    <h1>Types de biere index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomType</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for typebiere in typebieres %}
            <tr>
                <td>{{ typebiere.id }}</td>
                <td>{{ typebiere.nomType }}</td>
                <td>
                    <a href="{{ path('app_typebiere_show', {'id': typebiere.id}) }}">show</a>
                    <a href="{{ path('app_typebiere_edit', {'id': typebiere.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_typebiere_new') }}">Create new</a>
{% endblock %}
